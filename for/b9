#include <stdio.h>

int main(){
	int i,n;
	float s = 1;
	printf("nhap n: ");
	scanf("%d",&n);
	
	for(i = 2 ; i <= n ;i++){
		int a,gt =1 ;
		for(a = 1; a <= i;a++){
			gt = gt*a;
		}
		s = s + 1/(float)gt;
	}
	printf("tong s = %.4f",s);
}
